import React from 'react';

interface Props {

}

const Header = (props:Props) => {
    return (
        <header>
            <h1>React + TS Todo</h1>
        </header>
    )
};

export default Header;